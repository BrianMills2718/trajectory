# trajectory/extractors

Event and metadata extraction layers for `trajectory`.

This subtree owns git, Claude log, document, technology, dependency, and work
pattern extraction plus session building and concept rollups. Keep extraction
semantics explicit here.
