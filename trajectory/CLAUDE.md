# trajectory

Active `trajectory` implementation package.

This subtree contains config, models, database logic, ingestion, CLI, MCP
server, extractors, analysis, and output generation. Keep package-local
contracts here rather than at repo root.
